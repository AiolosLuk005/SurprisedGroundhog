allowed_roots = ["C:/", "D:/", "E:/", "F:/", "G:/"]
port = 5005
enable_hash = false
page_size_default = 50
ollama = { enable = true, model = "llama3.1:latest", timeout_sec = 120 }

# 放在全局（不在 [mysql] 里）
trash_dir = "D:/SurprisedGroundhog_Trash"

[mysql]
enable = true        # ← 修正这里（原来是 ture）
host = "127.0.0.1"
port = 3306
user = "root"
password = "mem@MS0ry"
database = "groundhog"

[logging]
# 日志级别: DEBUG, INFO, WARNING, ERROR
level = "INFO"
# 为空则输出到控制台，否则输出到指定文件
file = ""

